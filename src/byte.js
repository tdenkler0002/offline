class Byte {
    constructor(x, y) {
        this.width;
        this.height;

        this.canvas = document.createElement('canvas');
        this.context = canvas.getContext('2d');
    }

    draw() {
        
    }

    jump() {

    }
}